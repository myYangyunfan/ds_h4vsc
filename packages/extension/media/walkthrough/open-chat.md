打开对话面板，看看能做点什么。

点击左侧活动栏的鲸鱼图标，或按 `Ctrl+Alt+D` 打开 **DeepSeek Harness** 对话面板。

- 空状态提供"快速开始"卡片：解释选中代码、编写单元测试、重构、初始化项目上下文
- 输入 `@` 附加文件，输入 `/` 使用斜杠命令（/init、/compact、/login、/help）
- 发送时会自动带上编辑器里的当前选区（可在设置中关闭）

Open the chat panel from the whale icon in the Activity Bar, or press `Ctrl+Alt+D`.
