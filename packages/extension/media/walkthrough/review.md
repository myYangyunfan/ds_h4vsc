像 Copilot 代理模式一样审查 AI 的每处改动。

- 代理修改文件后，面板底部出现**已变更文件**列表，点击文件名打开**原生 diff 视图**
- 逐个审查：点击"逐个审查"按钮或按 `Ctrl+Alt+R`，依次打开每个待审查变更
- 每个文件可单独 **接受 / 拒绝**，也可一键**全部保留**；命令执行等敏感操作会先弹出审批卡

Review every agent change with native diffs: open them one by one (`Ctrl+Alt+R`), then accept or reject per file.
